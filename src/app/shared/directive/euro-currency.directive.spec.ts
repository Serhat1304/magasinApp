import { EuroCurrencyDirective } from './euro-currency.directive';

describe('EuroCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new EuroCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
